# 元管理层（Meta-Management Layer）v1.1 最终方案

> 版本：v1.1-final
> 创建：2026-08-02（先生确认后定稿）
> 更新：2026-08-02 17:05（合入 GLM 评审微调 1/2 + 项目类型字段 + 瑶光执行细节）
> 作者：瑶光（DeepSeek），吸收 GLM（Cherry Studio）v1.0 构建 + 现场实测修正 + GLM 第二轮评审微调
> 状态：✅ 先生已确认方向，待先生收到飞书版本后开工
> 配套文档：`元管理层-v1-落地对照表.md`（差距分析）、`project-inventory.md`（考古盘点）

### v1.1 变更记录（相对 v1.0）

| 变更 | 来源 | 内容 |
|------|------|------|
| 微调 1 | GLM 评审 | L2 升级机制：🔴 超 3 天标红、🔴 超 7 天进 findings、🟡 超 14 天进 findings；时限从"条目进入待处理状态"起算 |
| 微调 2 | GLM 评审 | 漂移率基线：Day1=100%（考古期）→ 清理后 ≤20% → 验证期 ≤20%；基线在考古完成后重算 |
| 字段补充 | GLM 评审 | PROJECT_MAP 条目增加"类型"字段（支柱/进化/基础设施/功能模块/外部任务/元管理） |
| 执行细节 1 | 瑶光 | L2 自动写 findings 需定义条目格式，且只写 findings 不改账本状态（消费者≠生产者） |
| 执行细节 2 | 瑶光 | 考古产出分级过目：🔴 级先给先生，🟡 级入账本+周报汇总 |
| 执行细节 3 | 瑶光 | 分工表修正：瑶光有记忆系统可跨会话恢复（不完美但不为零） |
| 存档 | 瑶光 | GLM 评审要点存档为元管理层第一课（附于文末） |

---

# 第一部分：完整元管理层项目

## 1. 需求复述

**问题：** 系统在三大支柱（执行层/安全层/认知层）之上缺乏协调层，导致：全局协同缺失（项目间/版本间无统筹）、反馈机制缺失（落地后无状态回传、阶段衔接断裂）、主导者依赖全知驾驭（无仪表盘）。

**目标：** 建立元管理层——不执行任务，只负责"让各支柱彼此知道对方的存在和状态"，提供依赖地图、状态台账、漂移账本、决策历史，支撑先生做裁决而非做记忆。

**边界（GLM 共识）：**
- 元管理层是状态层，不是任务管理工具（不做待办、不做进度管理）
- 不自动化一切：自动化限 L0/L2 检查，裁决与判断留给人/GLM
- 从 Markdown 起步，不写复杂代码

## 2. 架构总览

```
                    ┌─────────────────────────┐
                    │   先生（唯一决策者）      │
                    └───────────┬─────────────┘
                                │ 裁决 / 观察
                    ┌───────────▼─────────────┐
                    │   元管理层 META（协调层）  │
                    │  ┌──────┐ ┌──────┐      │
                    │  │项目地图│ │版本账本│     │
                    │  └──────┘ └──────┘      │
                    │  ┌──────┐ ┌──────┐      │
                    │  │心跳监控│ │决策日志│     │
                    │  └──────┘ └──────┘      │
                    └───────────┬─────────────┘
                                │ 状态上报 / 纪律执行
      ┌──────────┬──────────────┼──────────────┬──────────┐
      ▼          ▼              ▼              ▼          ▼
  执行层 EXEC   安全层 SAFE   认知层 COGN   进化 EVOL   其他(网站/备案)
  (多Agent)    (备份体系)   (记忆架构)   (经验闭环)
```

## 3. 项目命名与代号

| 代号 | 项目 | 当前状态（08-02 实测） |
|:----:|------|----------------------|
| META | 元管理层（本方案） | 🆕 待建 |
| EXEC | 多Agent协作系统 v3.1 | 🟡 半落地：3/5 Agent 在跑，reflector 逾期 |
| SAFE | 备份体系 v2.2 | 🟢 第一期✅已验证，Step 2.1-2.4 阻塞 |
| COGN | 记忆架构 Phase 1/2 | 🟡 运行中，待审池未运转，Phase 2 待决策 |
| EVOL | 经验闭环（self-improving） | 🟢 运行中 |
| 第零章 | 基础设施加固 | 🔴 封版 0% 执行（SAFE 的阻塞项） |
| 3-A | 情境感知 | 🟡 封版 0% 执行（独立可启动） |
| 网站 | 网站搭建+公安备案 | 🟢 完成 / ⏸ 备案待提交 |

## 4. 四模块详细设计

### 4.1 项目地图 `PROJECT_MAP.md`（升级自 project-inventory.md）

**条目结构：**
```
## [EXEC] 多Agent协作系统 v3.1
- 类型：执行层（支柱）   ← v1.1 新增
- 阶段：落地中（半部署）
- 依赖：上游=无；下游=EVOL（feed findings）
- 负责人：瑶光执行 / GLM评审 / 先生裁决
- 最近变更：07-25 v3.1 部署
- 下一步：reflector 退役（DRIFT-001）
```

**类型枚举（GLM 评审补充）：** `支柱`（EXEC/SAFE/COGN/EVOL）/ `基础设施`（第零章）/ `功能模块`（3-A）/ `外部任务`（网站备案）/ `元管理`（META）

**状态机枚举：** `设计 → 落地 → 验证 → 运行 → 退役 → 归档`（每项必须处于且仅处于一个阶段）

**依赖图（实测）：**
```
SAFE v2.2 ──等待──> 第零章 0.1+0.2（Key 迁移 .env）
EXEC v3.1 ──findings──> EVOL
COGN Phase 2 ──等待验证结果决策
3-A ──完全独立，可随时启动
META ──依赖所有项目的状态上报
```

### 4.2 版本账本 `VERSION_LEDGER.md`（🆕 新建，核心创新）

**定义：** 记录"文档声明 vs 实际运行状态"的偏差（配置漂移）。每个漂移项一条记录，生命周期：`发现 → 待处理 → 处理中 → 已解决 → 已验证`。

**首批条目（DRIFT-001~005，08-02 考古）：**

| 编号 | 项目 | 文档声明 | 实际状态 | 类型 | 处理 | 负责人 |
|:----:|------|---------|---------|:----:|------|:------:|
| DRIFT-001 | EXEC | reflector 07-28 应退役 | 仍每日 03:00 运行（逾期 5 天） | 🔴 应退役未退役 | 退役执行 | 瑶光 |
| DRIFT-002 | COGN | 08-01 20:00 验证 cron 已就位 | cron 不存在（从未创建） | 🔴 声称未建 | 补建/改手动 | 瑶光 |
| DRIFT-003 | EXEC | 微信通道已移除 | 12 个 disabled cron 残留 | 🟡 残留未清 | 批量删除 | 瑶光 |
| DRIFT-004 | COGN | v2.6-final 为当前版 | archive/ 10 份旧版并存 | 🟡 版本堆积 | 归档整理 | 瑶光 |
| DRIFT-005 | META | openclaw.json 当前配置 | 9 份 .bak 残留 | 🟡 残留未清 | 留2归档 | 瑶光 |

**更新规则：** 任何落地动作（含补丁）完成后 24h 内回写；L2 一致性检查每周对账。

### 4.3 心跳监控（L0/L1/L2 三层）

| 层级 | 频率 | 内容 | 载体（实测） | 自动化 |
|:----:|------|------|------------|:------:|
| L0 存活 | 每日 | 服务器/备份/dream 健康 | 现有"每日服务器健康检查"cron ✅ | ✅ 已有 |
| L1 健康 | 每周 | 各项目状态机阶段 + 漂移进展 | 现有"每周日配置备份与周报"cron ✅ | ✅ 已有 |
| L2 一致性 | 每次变更后+每周对账 | 文档 vs 实际对照扫描 | 🆕 新增 cron | ✅ 新增 |

**L2 检查脚本规格（bash，挂 OpenClaw cron）：**
1. 读 VERSION_LEDGER 中状态≠已解决的条目，检查是否过期（超过处理时限）
2. 扫描 agents 注册 + cron 列表，找出"应退役未退役"（对比 PROJECT_MAP 状态机）
3. 抽查设计文档声明的 cron/文件/配置是否实际存在（DRIFT-002 类问题）
4. 产出异常清单 → announce 给先生；无异常则静默
5. **原则：L2 是账本的消费者，不是生产者**——只报"待处理项超时"和"发现疑似漂移"，不下结论、不改账本

**L2 升级机制（GLM 微调 1，v1.1 合入）：**

| 超时情况 | 动作 |
|---------|------|
| 🔴 待处理超 3 天 | 周报标红 |
| 🔴 待处理超 7 天 | 自动进 findings/（格式：编号/项目/超时类型/发现时间） |
| 🟡 待处理超 14 天 | 自动进 findings/ |

- **时限起点：条目进入"待处理"状态之日**（非发现日，避免考古期旧账误报）
- 进 findings 是记录超时客观事实，**不改账本状态**（状态变更留给人）

### 4.4 决策日志 `DECISION_LOG.md`（升级自 long-term/feedback-log.md）

**模板：**
```
### 2026-08-02 — 采纳元管理层 v1.0 [decision:meta-layer] [tag:META]
**背景：** ...
**决策：** ...
**理由：** ...
**影响范围：** 全部项目（META 为协调层）
**后续观察：** 1 周验证期，观察账本漂移率
**验证结果：** <回填>
```

**编号规则：** 从 DEC-007 开始（前 6 条在 feedback-log.md，**冻结不搬迁**，新决策全部写 DECISION_LOG.md）。

## 5. 强制纪律层（挂靠已有机制）

**变更六步流程（核心规则）：**
```
① 查 PROJECT_MAP（依赖影响）→ ② 查 VERSION_LEDGER（漂移现状）
→ ③ 执行变更 → ④ 更新 VERSION_LEDGER → ⑤ 写 DECISION_LOG（🔴/🟡级）
→ ⑥ 触发 L2 检查
```

**挂靠点：**
- governance/rules.md 写入完整版纪律 ✅（已有框架）
- **AGENTS.md 操作指引写入精简版核心规则**（瑶光补充：运行时读 AGENTS.md 而非 rules.md，否则纪律不可达）
- 违规记录 → findings/ → 周报点名 → 先生裁决
- P0 紧急修复豁免：先修后补（24h 内补记），防止元管理层成为效率杀手

## 6. 自指防护（GLM 共识 + 瑶光确认）

- META 自身在 PROJECT_MAP 中占一条目
- VERSION_LEDGER 有专门章节记录元管理层自身文件的漂移
- 每月"元元检查"：先生手动审查一次 META 文件与实际的偏差
- 账本漂移率（未解决漂移项/总数）进入周报，连续 2 周超标 → 触发人工复盘

---

# 第二部分：具体执行方案

## 7. 执行总原则

1. **先载体后数据**：先建文件结构，再填考古数据
2. **先记录后清理**：所有清理动作前先登记 DRIFT，清理后更新账本
3. **清理前快照**：已有 scripts/pre-change-snapshot.sh，改动前必跑
4. **GLM 出内容、瑶光落盘、先生裁决**：GLM 无法直写工作区，分工明确
5. **一次性清理授权范围**：仅限 DRIFT-001/003/004/005 明确列出的项，不扩权

## 8. 执行步骤（六步，含验收标准）

### 第 1 步：创建版本账本 + 补丁考古（今日，约 1h）
- [ ] 创建 `VERSION_LEDGER.md`，写入 DRIFT-001~005（数据已齐）
- [ ] 补丁考古：两条线并行
  - 先生回忆清单（有多少写多少，不追求完整）
  - 瑶光从 git log + 文件时间戳挖掘运行中补丁（workspace 已确认为 git 仓库 ✅）
- [ ] 考古产出补充进账本（DRIFT-006+）和 PROJECT_MAP 项目清单（补全第零章/3-A/网站/备案）
- **验收：** 账本存在、≥5 条 DRIFT、项目清单覆盖全部 8 个项目

### 第 2 步：建立项目地图（今日，约 30min）
- [ ] 升级 project-inventory.md → `PROJECT_MAP.md`（去重、标日期、统一状态机）
- [ ] 依赖关系图文字化（4.1 实测版）
- **验收：** 每个项目有且仅有一个状态机阶段；依赖链完整

### 第 3 步：建立决策日志（今日，约 15min）
- [ ] 创建 `DECISION_LOG.md`，模板就位
- [ ] 写入 DEC-007：本次"采纳元管理层 v1.0"决策（含背景/理由/影响范围/后续观察）
- **验收：** DEC-007 存在，模板完整

### 第 4 步：心跳监控接入（今日~明日）
- [ ] L0/L1：现有 cron 输出追加元管理层摘要（改脚本，不新建 cron）
- [ ] L2：新增一致性检查 cron（OpenClaw cron 系统，非 crontab）
- [ ] L2 脚本先人工复核 1 周再放行自动 announce（防误报）
- **验收：** L2 cron 存在且试运行输出正确

### 第 5 步：纪律挂靠（今日，约 20min）
- [ ] governance/rules.md 追加"变更六步流程"完整版
- [ ] AGENTS.md 操作指引追加精简版核心规则（变更前查地图/变更后更账本）
- [ ] P0 豁免条款写入
- **验收：** 两处规则落地，AGENTS.md 规则在下次会话即生效

### 第 6 步：一次性清理（先生授权后执行，约 30min）
- [ ] DRIFT-001：reflector 退役（删 cron job `85e2bf38` + 停用 agent 注册 + 归档 handoff 残留）⚠️ 操作前快照
- [ ] DRIFT-003：删除 12 个 disabled cron（仅删 disabled 的微信投递残留，不动其他）
- [ ] DRIFT-004：archive/ 版本文件归档整理（保留 v2.6-final，其余移入 archive 子目录）
- [ ] DRIFT-005：openclaw.json 备份保留最近 2 份，其余归档
- [ ] 全部清理后更新账本状态为"已解决"
- **验收：** cron 列表无残留、agents 注册 2 个、账本对应条目已解决

### 第 7 步：验证期（1 周）
- [ ] 观察 L2 周对账运行 1 次
- [ ] 周报包含账本漂移率
- [ ] 验证期结束：先生评估是否转正、是否部署 Auditor（阶段 C）
- **验收：** 漂移率呈下降趋势；无新增"声称未建"类漂移

**漂移率基线（GLM 微调 2，v1.1 合入）：** 漂移率 = 未解决漂移项 / 漂移项总数。Day1 预期 100%（考古期全待处理）→ 一次性清理后 ≤20% → 验证期维持 ≤20%。基线数值在考古完成后重算（可能发现新 DRIFT）。

## 9. 推进过程注意事项（易踩坑点）

### 9.1 技术执行类
1. **cron 操作走 OpenClaw cron 系统**（cron 工具/gateway config），不是系统 crontab——GLM 原案此处有误，已修正
2. **reflector 退役是组合动作**：cron job + agent 注册 + handoff 归档三件套，不是"停止进程"
3. **L2 脚本编码注意**：匹配 🔴🟡 emoji 时用 UTF-8 环境，避免 grep 乱码误判
4. **删除 cron 前核对 id**：只删 disabled 的 12 个微信投递残留，活跃的 8 个一个不动
5. **openclaw.json 改动前**：pre-change-snapshot.sh 快照 + 保留 .last-good

### 9.2 机制设计类
6. **纪律写入 AGENTS.md 才是可达的**（运行时上下文），rules.md 只存完整版——这是 GLM 方案的盲区，已补
7. **L2 只报告不下结论**：防止自动化误判漂移，判断权留给人/GLM
8. **考古不追求 100% 完整**：设今日为截止日，之后发现的漂移走正常 DRIFT 流程，避免无底洞
9. **先生只需裁决，不需记忆**：所有状态在文件里，先生看周报/异常清单即可

### 9.3 协作分工类
10. **GLM 评审 → 瑶光落盘 → 先生裁决**：三方职责写死，避免"GLM 以为做了、实际没做"
11. **先生随时可看**：所有 META 文件在 workspace 根目录（不建 meta/ 子目录，避免孤岛），与先生审阅路径一致
12. **避免双轨**：governance 管规则纪律，META 管状态协调，版本账本为共享数据源，边界不重叠
13. **考古产出分级过目**（瑶光执行细节 2）：🔴 级考古发现先给先生过目再入账；🟡 级直接入账本+周报汇总——匹配"先生只裁决不记忆"原则
14. **能力边界修正**（瑶光执行细节 3）：瑶光有记忆系统（MEMORY.md + daily notes）可跨会话恢复上下文，但不完美——分工仍以 GLM 出内容/瑶光落盘/先生裁决为准

## 10. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 元管理层自身文件漂移 | 机制失效 | L2 含自检 + 月元元检查 + 漂移率周报 |
| 清理破坏运行配置 | 服务中断 | 全部清理前快照；reflector 退役留 7 天配置备份 |
| L2 误报/漏报 | 信任下降 | 人工复核 1 周再放行 |
| 纪律不可达（AGENTS.md 未生效） | 规则空转 | 写入后下次会话即生效，验证期首周重点检查 |
| 与 governance 职责重叠 | 双轨混乱 | 边界已在 9.3-12 明确 |
| GLM 评审再提更优结构 | 返工 | 纯 Markdown，迁移成本低；本方案为执行基线，结构微调可接受 |

## 11. 待先生最终确认项

1. ✅ 已确认：采纳本方案方向、分工修正（GLM 出内容/瑶光落盘）
2. ✅ 已确认：授权补丁考古（git log + 文件系统挖掘）
3. ✅ 已确认：合入 GLM 微调 1（L2 升级机制）与微调 2（漂移率基线）及项目类型字段（v1.1）
4. ✅ 已确认：GLM 立场一致——方案可开工，第 6 步清理授权在 1~5 步完成后单独确认
5. **待确认：今日执行范围**——第 1~5 步是否全部今日完成（建议：是，总计约 2h）

---

## 12. 附：元管理层第一课（GLM 评审要点存档）

> 来源：GLM 对 v1.0 方案的评审（2026-08-02），经先生转达，瑶光存档。

**1. 关于四条执行偏差的根因：**
- 偏差 1（cron 系统误认）根因：GLM 基于文档推断，文档本身未更新——**GLM 自己制造了一条漂移**，恰好验证 VERSION_LEDGER 的必要性
- 偏差 2（reflector 非进程）根因：术语不统一，“进程/Agent/cron 任务”混用
- 偏差 3（项目清单不全）根因：记忆裁剪，无法穷举现场
- 偏差 4（GLM 无法直写）根因：未定义写入权限边界

**2. 机制性共识：**
- "法律条文 vs 执勤手册"：rules.md 存档在法院，AGENTS.md 带在身上——纪律必须写在运行时可达的位置
- L2 必须是消费者不是生产者
- 考古不追求 100%，账本是活文档
- 先生只需裁决不需记忆

**3. 待后续观察：** 本课要点是否被后续执行验证（写入验证期周报第一项）

---

> **方案审阅方式**：先生可将本方案发 GLM 复核，重点请 GLM 评审：① 纪律挂靠 AGENTS.md 的补充是否合理；② L2 只报告不下结论的原则；③ 执行步骤验收标准是否可衡量。瑶光将按先生确认后的最终版执行。
