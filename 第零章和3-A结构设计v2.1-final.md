# 第零章和3-A结构设计 v2.1-final

> 封版日期：2026-07-29
> 设计：瑶光（DeepSeek） | 评审：GLM-5.1 | 终审：贾先生

---

## 一、概述

本文档定义瑶光"全能私人管家"蓝图的前两个具体实施单元：

- **第零章（Chapter 0）**：基础设施加固 — API Key 环境变量化、飞书签名验证、安全基线
- **3-A（Chapter 3-A）**：情境感知 — 天气 + 日程的实时 Context 构建

两个单元可并行推进，不相互阻塞。3-A 因选用 wttr.in（无需 API Key），可在第零章完成前独立启动。

---

## 二、第零章：基础设施加固

### 0.1+0.2 API Key 环境变量化

**目标：** 将 openclaw.json 中的明文 API Key 迁移到环境变量，通过 `${VAR_NAME}` 占位符引用（OpenClaw 原生支持）。

**步骤：**

1. **备份 openclaw.json（先在 workspace 外创建快照，锁定 git 安全）**
   ```bash
   mkdir -p ~/.openclaw/snapshots
   mkdir -p ~/.openclaw/workspace/config-backup
   echo "config-backup/" >> ~/.openclaw/workspace/.gitignore
   echo "snapshots/" >> ~/.openclaw/workspace/.gitignore
   cd ~/.openclaw/workspace && git add .gitignore && git commit -m "chore: add backup dirs to gitignore"
   SNAP_DATE=$(date +%Y%m%d_%H%M)
   cp ~/.openclaw/openclaw.json ~/.openclaw/snapshots/openclaw.json.${SNAP_DATE}
   cp ~/.openclaw/openclaw.json ~/.openclaw/workspace/config-backup/openclaw.json.bak
   ```
   > ⚠️ `.gitignore` 必须在任何 `cp` 进 workspace 之前更新，防止明文 Key 被 git 追踪。

2. **提取所有 API Key 字段**
   ```bash
   python3 -c "
   import json
   with open('/home/ubuntu/.openclaw/openclaw.json') as f:
       config = json.load(f)
   keys = set()
   # Providers
   for p in config.get('providers', []):
       for k in ['apiKey', 'secretKey', 'appSecret', 'token', 'password']:
           if k in p:
               keys.add(('PROVIDER_' + p.get('name', p.get('model', 'UNKNOWN')).upper().replace('-','_').replace('.','_'), p[k]))
   # Channels
   for c in config.get('channels', []):
       accounts = c.get('accounts', {})
       if isinstance(accounts, dict):
           for an, ac in accounts.items():
               for k in ['appSecret', 'botToken', 'token', 'password']:
                   if k in ac:
                       keys.add(('CHANNEL_' + c.get('name', 'UNKNOWN').upper() + '_' + an.upper() + '_' + k.upper(), ac[k]))
   for name, val in sorted(keys):
       print(f'{name}={val}')
   "
   ```

3. **设置环境变量（写入 shell rc）**
   ```bash
   # 追加到 ~/.bashrc
   for var in $(python3 extract_keys.py); do
       echo "export $var" >> ~/.bashrc
   done
   source ~/.bashrc
   ```

4. **用 `${VAR_NAME}` 替换 openclaw.json 中的明文值**
   ```bash
   python3 -c "
   import json
   with open('/home/ubuntu/.openclaw/openclaw.json') as f:
       config = json.load(f)
   
   # 替换 providers
   for p in config.get('providers', []):
       for k in ['apiKey', 'secretKey', 'appSecret', 'token', 'password']:
           if k in p and p[k]:
               var_name = 'PROVIDER_' + p.get('name', p.get('model', 'UNKNOWN')).upper().replace('-','_').replace('.','_')
               p[k] = f'\${{{var_name}}}'
   
   # 替换 channels
   for c in config.get('channels', []):
       accounts = c.get('accounts', {})
       if isinstance(accounts, dict):
           for an, ac in accounts.items():
               for k in ['appSecret', 'botToken', 'token', 'password']:
                   if k in ac and ac[k]:
                       var_name = 'CHANNEL_' + c.get('name', 'UNKNOWN').upper() + '_' + an.upper() + '_' + k.upper()
                       ac[k] = f'\${{{var_name}}}'
   
   with open('/home/ubuntu/.openclaw/openclaw.json', 'w') as f:
       json.dump(config, f, indent=2, ensure_ascii=False)
   "
   ```

5. **验证语法正确**
   ```bash
   python3 -m json.tool ~/.openclaw/openclaw.json > /dev/null && echo "JSON valid"
   ```

6. **重启 Gateway 验证**
   ```bash
   openclaw gateway restart --reason "API Key 环境变量化迁移"
   ```

7. **确认所有 provider/channel 正常运行**
   - 检查日志：`journalctl -u openclaw --since "5 minutes ago"`
   - 发送一条测试消息

**回滚方案：**
```bash
cp ~/.openclaw/snapshots/openclaw.json.${SNAP_DATE} ~/.openclaw/openclaw.json
openclaw gateway restart --reason "回滚 API Key 迁移"
```

---

### 0.3 飞书签名验证

**目标：** 确保飞书事件回调的 Verification Token / 签名验证已启用。

**执行步骤：**
1. 登录飞书开发者后台 → 事件回调 → 查看 Verification Token 是否已配置
2. 如果未配置 → 开启签名验证 → 更新 openclaw.json 中飞书通道的 token 配置
3. 发送一条测试消息 → 确认验证通过
4. 用 curl 模拟一个无签名的请求 → 确认被拒绝

**验收标准：**
- 模拟伪造请求被拒绝
- 合法请求通过

---

### 0.4+0.5 白名单 + 审计

**目标：** 确认飞书通道已配置群组白名单，确认 Gateway 审计日志已启用。

**步骤：**
- 检查 `openclaw.json` 中 feishu 通道的 `groupAllowFrom` 配置
- 确认审计日志文件存在且写入正常
- 记录审计日志路径和轮转策略

---

## 三、3-A：情境感知（Weather + Context Builder）

### 架构选择

| 组件 | 技术选型 | 理由 |
|------|---------|------|
| 天气数据 | wttr.in | 唯一免费开源、无需API Key、可在第零章前独立启动 |
| 日程存储 | YAML（schedule.yaml） | 人类可读写、版本可控、无数据库依赖 |
| Context 生成 | Python 脚本（context-builder.py） | 轻量、可独立测试、不依赖框架 |
| 注入策略 | 三阶段递进 | 先手动验证 → 后按需调用 → 再自动注入 |

### schedule.yaml

```yaml
# schedule.yaml
# 未来扩展：支持 repeat 字段（daily/weekly/monthly），Phase 2 实现
# 示例：- repeat: weekly, day: monday, time: "09:00", title: "站会"

timezone: Asia/Shanghai
events:
  - date: "2026-07-29"
    time: "19:00"
    title: "和同事吃饭"
```

### context-builder.py

```python
#!/usr/bin/env python3
"""
情境信息构建脚本 — 天气 + 当前时间 + 今日日程
输出格式：标准文本块，供瑶光作为系统上下文注入
"""

import json
import urllib.request
import datetime

TIMEZONE = "Asia/Shanghai"

def get_time_context():
    now = datetime.datetime.now()
    weekdays = ["星期一", "星期二", "星期三", "星期四", "星期五", "星期六", "星期日"]
    return {
        "datetime": now.strftime("%Y-%m-%d %H:%M"),
        "weekday": weekdays[now.weekday()],
        "hour": now.hour
    }

def get_weather():
    """从 wttr.in 获取天气（简约格式）"""
    url = "https://wttr.in/Shanghai?format=j1"
    try:
        req = urllib.request.urlopen(url, timeout=10)
        data = json.loads(req.read())
        current = data["current_condition"][0]
        return {
            "temp": current["temp_C"],
            "feels_like": current["FeelsLikeC"],
            "humidity": current["humidity"],
            "weather_desc": current["weatherDesc"][0]["value"],
            "wind_speed": current["windspeedKmph"],
            "wind_dir": current["winddir16Point"]
        }
    except Exception as e:
        return {"error": f"天气数据获取失败: {e}"}

def get_schedule():
    """解析 schedule.yaml（当前简单实现，Phase 2 支持重复日程）"""
    try:
        import yaml
        with open("schedule.yaml", "r") as f:
            data = yaml.safe_load(f)
    except FileNotFoundError:
        return {"events": []}
    except Exception:
        return {"events": []}
    
    now = datetime.datetime.now()
    today = now.strftime("%Y-%m-%d")
    hour = now.hour
    minute = now.minute
    
    today_events = []
    for event in data.get("events", []):
        if event["date"] == today:
            event_hour, event_min = map(int, event["time"].split(":"))
            # 返回未来3小时内+当前已开始的日程
            if (event_hour > hour or (event_hour == hour and event_min >= minute)) and \
               (event_hour <= hour + 3):
                today_events.append(event)
    
    return {"events": today_events}

def build_context():
    """组装完整情境信息"""
    
    now_ts = datetime.datetime.now().strftime("%Y-%m-%d %H:%M")
    
    lines = []
    lines.append(f"情境信息: [更新于 {now_ts}]")
    lines.append("")
    
    # 时间
    time_ctx = get_time_context()
    lines.append("## 📅 当前时间")
    lines.append(f"- {time_ctx['datetime']} {time_ctx['weekday']}")
    lines.append("")
    
    # 天气
    weather = get_weather()
    if "error" not in weather:
        lines.append("## 🌤️ 天气")
        lines.append(f"- {weather['weather_desc']}，{weather['temp']}°C（体感 {weather['feels_like']}°C）")
        lines.append(f"- 湿度 {weather['humidity']}%，{weather['wind_dir']}风 {weather['wind_speed']}km/h")
    else:
        lines.append(f"## 🌤️ 天气")
        lines.append(f"- {weather['error']}")
    lines.append("")
    
    # 日程
    schedule = get_schedule()
    if schedule["events"]:
        lines.append("## 📋 今日日程")
        for e in schedule["events"]:
            lines.append(f"- {e['time']} {e['title']}")
    else:
        lines.append("## 📋 今日日程")
        lines.append("- 暂无近期日程安排")
    lines.append("")
    
    # 时段问候
    hour = time_ctx["hour"]
    if hour < 6:
        lines.append("💡 深夜时段，瑶光应安静待命，除非紧急事件不主动打扰")
    elif hour < 9:
        lines.append("💡 早晨时段，可主动问候和汇报日程")
    elif hour < 12:
        lines.append("💡 上午时段，正常工作模式")
    elif hour < 14:
        lines.append("💡 午间时段")
    elif hour < 18:
        lines.append("💡 下午时段，正常工作模式")
    elif hour < 22:
        lines.append("💡 晚间时段")
    else:
        lines.append("💡 深夜时段，除非紧急不主动打扰")
    
    return "\n".join(lines)

if __name__ == "__main__":
    print(build_context())
```

### 三阶段注入策略

| Phase | 方式 | 目标 |
|-------|------|------|
| **Phase 1** | 手动运行脚本，观察输出正确性 | 验证天气获取、日程解析、输出格式 |
| **Phase 2** | 瑶光 skill 调用 context-builder.py | 按需注入情境信息 |
| **Phase 3** | 自动注入到每次对话的系统上下文 | 全自动情境感知 |

> **Phase 2 集成方式：** 采用 OpenClaw skill 机制。已有 weather skill 可作为参考模板。Phase 1 验证通过后，创建自定义 skill 集成到瑶光的 55 个现有技能体系中。Phase 3 的结构在 Phase 1 代码中预埋（通过 `__name__ == "__main__"` 保持模块可导入）。

### 明确不做（边界清单）

| 不做 | 理由 |
|------|------|
| 不做实时天气推送 | 属于 6-B（主动通知）的范围 |
| 不做自动学习日程模式 | 属于 6-B 的范围，手动声明更可靠 |
| 不做日历系统（增删改） | 只需读取，不提供写能力 |
| 不做复杂自然语言时间解析 | Phase 1 保持 YAML 格式简单 |

---

## 四、完整执行时序

```
时间轴 →

T0（今晚）
  │
  ├── P0 备份（先做！）
  │   ├── 创建 snapshots/ (workspace 外) + .gitignore
  │   ├── openclaw.json → snapshots/ + config-backup/
  │   ├── Nginx + crontab → config-backup/
  │   └── yg-knowledge push
  │
  ├── 3-A Phase 1 开始（并行）
  │   ├── 创建 schedule.yaml
  │   ├── 编写 context-builder.py
  │   └── 手动测试脚本输出
  │
T1（明天）
  │
  ├── 0.1+0.2 API Key 环境变量化
  │   └── 验证重启后所有 provider 正常
  │
  ├── 3-A Phase 1 继续（并行）
  │   └── 调试 wttr.in 回退处理 + schedule 解析
  │
T2（后天）
  │
  ├── 0.3 飞书签名验证
  ├── 0.4+0.5 白名单 + 审计
  │
  ├── 3-A Phase 2 集成（第零章已完成，可接真实数据）
  │   └── 瑶光 skill 调用 context-builder.py
  │
T3（验证日）
  │
  ├── 第零章全体验收
  ├── 3-A Phase 2 验收
  └── 消防演习：模拟 openclaw.json 丢失 → 从快照恢复
```

---

## 五、P0 备份完整执行清单

```bash
# ═══════════════════════════════════════════
#  P0 备份执行清单 — 从这里开始
# ═══════════════════════════════════════════

# ── Step 1: 创建备份目录 ──────────────────
mkdir -p ~/.openclaw/snapshots
mkdir -p ~/.openclaw/workspace/config-backup

# ── Step 2: 锁住 git 安全门 ──────────────
echo "config-backup/" >> ~/.openclaw/workspace/.gitignore
echo "snapshots/" >> ~/.openclaw/workspace/.gitignore
cd ~/.openclaw/workspace && git add .gitignore && git commit -m "chore: add backup dirs to gitignore"

# ── Step 3: 备份 openclaw.json ────────────
SNAP_DATE=$(date +%Y%m%d_%H%M)
cp ~/.openclaw/openclaw.json ~/.openclaw/snapshots/openclaw.json.${SNAP_DATE}
cp ~/.openclaw/openclaw.json ~/.openclaw/workspace/config-backup/openclaw.json.bak

# ── Step 4: 验证备份存在 ──────────────────
ls -la ~/.openclaw/snapshots/
ls -la ~/.openclaw/workspace/config-backup/

# ── Step 5: 备份 Nginx ────────────────────
sudo cp /etc/nginx/sites-available/jxpyaoguang ~/.openclaw/workspace/config-backup/

# ── Step 6: 备份 crontab ──────────────────
crontab -l > ~/.openclaw/workspace/config-backup/crontab.txt

# ── Step 7: yg-knowledge 推送 ─────────────
cd ~/yg-knowledge && git add -A && git commit -m "auto init $(date +%Y%m%d)" && git push

# ── Step 8: 验证全部备份 ──────────────────
echo "=== 备份验证 ==="
echo "openclaw.json 快照:" && ls ~/.openclaw/snapshots/openclaw.json.*
echo "openclaw.json config-backup:" && ls ~/.openclaw/workspace/config-backup/openclaw.json.bak
echo "Nginx:" && ls ~/.openclaw/workspace/config-backup/jxpyaoguang
echo "Crontab:" && wc -l ~/.openclaw/workspace/config-backup/crontab.txt
echo "yg-knowledge remote:" && cd ~/yg-knowledge && git remote -v
echo "=== P0 备份完成 ==="
```

---

## 六、变更记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v2.0 | 2026-07-28 | 初版方案 |
| v2.1 | 2026-07-29 GLM评审 | 修正 snapshots/ git 安全问题 |
| v2.1-final | 2026-07-29 | 封版（执行入口补全 + 重复日程暂缓说明 + 新鲜度标记） |

---

## 七、评审记录

- **GLM-5.1 评审结论：** 第零章 ✅可执行（需修正 snapshots 位置）| 3-A ✅可执行（建议加新鲜度标记 + Phase 2 集成预埋）| 执行时序 ✅ 正确
- **瑶光补充：** 重复日程暂缓至 Phase 2，schedule.yaml 中留注释标记扩展位 | 补全 P0 备份执行入口清单
- **GLM 回应：** 完全接受以上两点，并提供完整 exec checklist
- **贾先生终审：** ✅ 先出文档，后执行落地
