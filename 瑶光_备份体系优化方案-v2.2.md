# 瑶光·备份体系优化方案

> 版本：v2.2（基于 final × GLM 评审 × DeepSeek 微调，先生确认）
> 日期：2026-07-29
> 前置：三层三地备份架构 + 安全基线的备份部分
> 
> 变更说明：v2.2 修正了 v1 (final) 中的路径安全隐患（/tmp/ 重启丢失）、
> 补充 Obsidian Vault 备份遗漏、增加 SQLite 事务一致性和 `.env` 密钥托管架构、
> 强化验证步骤，详见文末变更日志

---

## 一、事故发生背景

2026 年 7 月初，云端瑶光在修改配置时因 Key 配置错误导致断联。本地瑶光尝试修复失败，先生过度信任瑶光能力延误了介入时机。最终不得不重新在云端部署，损失了一部分瑶光与先生之间的记忆。

此事故直接催生了：
- 每日 GitHub 自动备份（workspace git add -A + push）
- 每日 03:00 tar 打包（memory/ / long-term/ / transcripts/ / scripts/）
- 多 Agent 协作系统

但当前备份体系仍有致命盲区。

---

## 二、当前备份状态盘点

### ✅ 已有覆盖

| 项 | 方式 | 评价 |
|----|------|------|
| workspace 代码/配置 | `git add -A` + `git push` 每日 04:00 | 好 |
| 每日日志 memory/ | `git add -A` 覆盖 + 03:00 tar 覆盖 | 好 |
| 长期记忆 long-term/ | 同上 | 好 |
| 会话记录 transcripts/ | 03:00 tar 覆盖（sessions.db） | 好 |
| Dreaming 候选 | `git add -A` 覆盖 | 好 |

### ❌ 致命盲区（按优先级排序）

| 优先级 | 盲区项 | 位置 | 影响说明 |
|--------|--------|------|---------|
| **P0** | **openclaw.json** | `~/.openclaw/openclaw.json` | **不在 workspace 内**，git 不跟踪，tar 不包含。所有模型配置、Agent 定义、API Key 全在里面。服务器挂掉 → 瑶光骨架丢失 |
| **P0** | **yg-knowledge 知识库** | `~/yg-knowledge/` | 已配 GitHub remote（yg-knowledge 仓库），但 **无自动推送 cron**，当前仅 1 次手动 commit。无自动备份 = 无备份 |
| **P1** | **Nginx 配置** | `/etc/nginx/sites-available/jxpyaoguang` | SSL/域名/反向代理配置不备份，重部署时需手动恢复 |
| **P1** | **Crontab 配置** | 系统 crontab | 每日备份和 tar 的 cron 未导出，重装时遗忘 |
| **P1** | **变更前快照** | 不存在 | 每次改配置前无自动存档，改坏了回退困难 |
| **P1** | **Obsidian Vault** | 已知存在但方案初版未纳入 | Obsidian 笔记内容是工作记录的重要来源，丢失不可逆 |
| **P1** | **`.env` 密钥托管文件** | `~/.openclaw/.env`（新建） | API Key 迁移到独立 `.env` 后，bashrc 不再直接持有 Key。`.env` 损坏 = 所有 provider 同时断联。需要与 openclaw.json 同等保护级别的加密备份 |
| **P2** | **API Key 加密** | openclaw.json 中明文 | 备份到 GitHub 后有泄露风险（私有仓库低概率，但存在） |
| **P2** | **恢复剧本** | 不存在 | 没有标准化的恢复流程文档 |
| **P2** | **消防演习** | 从未执行 | 恢复流程未经实际验证 |

---

## 三、三层三地备份架构

### Layer 1：变更前快照（新）

在每次修改 `openclaw.json` 或 `.env` 等关键配置前，自动触发：

```bash
cp ~/.openclaw/openclaw.json ~/.openclaw/workspace/snapshots/openclaw.json.$(date +%Y%m%d_%H%M%S)
cp ~/.openclaw/.env ~/.openclaw/workspace/snapshots/env.$(date +%Y%m%d_%H%M%S)
```

- 目录：`workspace/snapshots/`
- 保留期：30 天，过期自动删除
- 触发方式：手动（当前）或 Gateway hook（未来）

### Layer 2：每日全量备份（优化现有）

保留现有 3 层，补充。关键变更：**tar 输出从 `/tmp/` 改为 `~/.openclaw/backups/daily/`**（/tmp/ 重启即丢失），且 tar 前先对 sessions.db 执行 SQLite `.backup` 保证一致性。

| 时间 | 内容 | 方式 | 保留期 |
|------|------|------|--------|
| 03:00 | workspace 关键文件 + sqlite 一致性备份 | `sqlite3 .backup` → tar + gzip → `~/.openclaw/backups/daily/` | 7 天 |
| 04:00 | workspace git add -A + push | `daily-git-backup.sh` | 永久 |
| **新 04:30** | **openclaw.json + .env 副本** | `cp ~/.openclaw/openclaw.json ~/.openclaw/.env → workspace/config-backup/` | 永久（git 托管后） |
| **新 04:30** | **yg-knowledge auto-push** | `cd ~/yg-knowledge && git add -A && git commit && git push` | 永久 |
| **新 04:30** | **Nginx 配置 + crontab 导出** | cp + crontab -l → workspace/config-backup/ | 永久 |
| **新 04:30** | **Obsidian Vault 增量备份** | rsync 或 cp -rL → workspace/obsidian-backup/ | 永久（git 托管） |

> **SQLite 一致性保证：** 在 03:00 tar 前插入以下步骤，避免正在写入的 databases.db 在 tar 过程中损坏：
> ```bash
> sqlite3 ~/.openclaw/workspace/transcripts/sessions.db \
>   ".backup ~/.openclaw/backups/daily/sessions-$(date +%Y%m%d).db"
> # 然后将备份后的 sqlite 文件纳入 tar，而非 live db
> ```

> **config-backup/.gitignore 安全策略：**
> - 初始阶段（API Key 仍明文在 openclaw.json 中）：`.gitignore` 中保留 `config-backup/`，防止明文 Key 被推送到 GitHub
> - 迁移后（API Key 迁入 `.env`，openclaw.json 内无明文 Key）：**移除 `.gitignore` 中的 `config-backup/` 条目**，使 git 跟踪该目录，实现异地备份
> - 顺序约束：**先验证所有 provider 正常运行 → 再开锁推 git**。不可乱序

> **yg-knowledge 安全性：** `git add -A` 可能误推 workspace 中意外残留的敏感文件。
> 建议在 `~/yg-knowledge/` 下创建 `.gitignore`：
> ```
> *.key
> *.pem
> *.gpg
> secrets/
> ```

### Layer 3：周归档（新）

每周日 05:00 全量打包（含加密敏感文件），异地存档：

```bash
mkdir -p ~/.openclaw/backups/weekly/

# 敏感文件加密后入包
tar czf ~/.openclaw/backups/weekly/full-backup-$(date +%Y%m%d).tar.gz \
  ~/.openclaw/openclaw.json.gpg \
  ~/.openclaw/.env.gpg \
  ~/.openclaw/workspace/ \
  ~/yg-knowledge/ \
  /etc/nginx/sites-available/jxpyaoguang

# 加密：gpg --symmetric --cipher-algo AES256 openclaw.json
#       gpg --symmetric --cipher-algo AES256 .env
# 解密：gpg --decrypt openclaw.json.gpg > openclaw.json
#       gpg --decrypt .env.gpg > .env
```

- 保留期：90 天（12 个归档）
- 异地：GitHub (workspace) + GitHub (yg-knowledge) = 已异地
- GPG 密码：先生保管（仅手动恢复时使用）

---

## 四、执行步骤（两步走）

### 第一步：立刻执行——先备份，再安全

**P0（立即，不开机就做）：**

| # | 任务 | 命令 |
|---|------|------|
| 0.1 | 创建 config-backup/ 目录 | `mkdir -p workspace/config-backup/` |
| 0.2 | 复制 openclaw.json | `cp ~/.openclaw/openclaw.json workspace/config-backup/openclaw.json` |
| 0.3 | 确认 openclaw.json 已进入 workspace | `ls -la workspace/config-backup/openclaw.json` |
| 0.4 | 建立 snapshots/ 目录 | `mkdir -p workspace/snapshots/` |
| 0.5 | yg-knowledge 加自动推送 cron | `cd ~/yg-knowledge && git add -A && git commit -m"auto $(date +%Y%m%d)" && git push` |
| 0.6 | 验证 git push 成功 | 查看 GitHub 仓库已更新 |
| **0.7** | 创建日常备份持久目录 | `mkdir -p ~/.openclaw/backups/daily/ ~/.openclaw/backups/weekly/` |

**P1（第一步内完成）：**

| # | 任务 |
|---|------|
| 1.1 | 导出 Nginx 配置到 config-backup/ |
| 1.2 | 导出 crontab 到 config-backup/ |
| 1.3 | 更新 daily-tar.sh：输出路径 /tmp/ → `~/.openclaw/backups/daily/` |
| 1.4 | 更新 daily-tar.sh：tar 前插入 SQLite `.backup` 步骤 |
| 1.5 | 更新 daily-git-backup.sh：加入 openclaw.json / .env / nginx / crontab / obsidian 新备份项 |
| 1.6 | 创建变更前快照脚本（含 openclaw.json + .env） |
| 1.7 | 在 workspace/.gitignore 中检查 config-backup/ 策略，确保 API Key 迁移前不暴露 |
| 1.8 | 为 yg-knowledge 添加 `.gitignore`（防误推敏感文件） |
| 1.9 | **验证**：执行一次 daily-tar.sh 模拟运行，检查文件大小符合预期 |

### 第二步：第一步之后

| # | 任务 | 说明 |
|---|------|------|
| 2.1 | API Key 加密存储 + `.env` 迁移 | 将 openclaw.json 中的 API Key 迁到 `~/.openclaw/.env`，bashrc 仅保留一行 `source`。移除 config-backup/ 的 .gitignore 条目（先验证全部 provider 正常） |
| 2.2 | 写 3 个恢复剧本 | Playbook A（配置损坏 < 2min）、B（GitHub 恢复 15-30min）、C（完全重建 1-2h） |
| 2.3 | emergency-recovery 技能 | 给本地瑶光写一个技能：读取 config-backup/ 恢复关键配置 |
| 2.4 | 消防演习 | 模拟一次断联 + 本地瑶光恢复全过程 |

> **关键原则：第一步先备份（含明文），第二步再清历史铲平安全风险。** 两害相权取其轻——私有仓库泄露 Key 的概率远低于服务器挂了没备份的风险。

---

## 五、三步执行验证（v2.2 新增）

> v2.2 在 "两步走" 架构基础上增加了三级验证护栏，防止静默失败。

### Step 8 — 验证 🔍

每个每日备份任务完成后，执行以下验证：

```bash
echo "=== 备份验证报告 $(date +%Y%m%d_%H%M%S) ==="

# 8.1 文件存在性检查
for f in \
  ~/.openclaw/backups/daily/workspace-$(date +%Y%m%d).tar.gz \
  ~/.openclaw/workspace/config-backup/openclaw.json \
  ~/.openclaw/workspace/config-backup/nginx-* \
  ~/.openclaw/workspace/config-backup/crontab-*; do
  if [ -f "$f" ]; then
    echo "✅ $(basename $f): $(ls -lh "$f" | awk '{print $5}')"
  else
    echo "❌ 缺失: $f"
  fi
done

# 8.2 最小文件大小检查（空文件 = 备份失败）
min_size=100
for f in ~/.openclaw/backups/daily/*.tar.gz; do
  size=$(stat -c%s "$f" 2>/dev/null || echo 0)
  if [ "$size" -lt "$min_size" ]; then
    echo "⚠️  文件过小: $(basename $f) = ${size}bytes"
  fi
done

# 8.3 git push 状态检查
cd ~/.openclaw/workspace
if git push --dry-run 2>&1 | grep -q "Everything up-to-date"; then
  echo "✅ git push: up-to-date"
elif git status --short | grep -q .; then
  echo "⚠️  有未推送变化，需要手动 git push"
fi

echo "=== 验证完毕 ==="
```

**验证失败措施：**
- 文件缺失 → cron 告警系统通知先生
- 文件过小 → 同上
- 验证结果写入 `~/.openclaw/backups/daily/verify-$(date +%Y%m%d).log`

---

## 六、三层三地架构总图

```
时间轴 → 变更前 → 每日 → 周日
           │        │       │
Layer 1   快照      │       │
(snapshot) cp JSON  │       │
           cp .env  │       │
           │        │       │
Layer 2    │      git push  │
(daily)    │      tar打包   │
           │      (sqlite)  │
           │      yg自动    │
           │      同步       │
           │      Obsidian  │
           │      同步       │
           │        │       │
Layer 3    │        │    全量加密
(weekly)   │        │    (含.env)
           │        │    异地存档
           │        │       │
保留期    30天      永久    90天
```

---

## 七、恢复剧本（待写）

| 剧本 | 场景 | 预计耗时 | 依赖 |
|------|------|---------|------|
| **A** | 配置改坏了但服务器还在 | < 2 分钟 | snapshots/ + git checkout |
| **B** | 服务器挂，GitHub 备份完整 | 15-30 分钟 | git clone + cp config-backup/ |
| **C** | 服务器挂 + 需从周归档恢复 | 1-2 小时 | Layer 3 GPG 解密 + 全量恢复 |

---

## 附录：v2.2 变更日志

| 变更 | 类型 | 来源 |
|------|------|------|
| tar 输出 `/tmp/` → `~/.openclaw/backups/daily/` | 🐛 安全修复 | GLM 评审 → DeepSeek 吸收 |
| SQLite `.backup` 一致性保证 | 🛡️ 可靠增强 | GLM 评审 → DeepSeek 吸收 |
| Obsidian Vault 备份补充 | ➕ 补充盲区 | DeepSeek 自检遗漏 |
| `.env` 密钥托管架构（新增独立备份项） | 🧱 架构变更 | GLM 评审（bashrc SPOF）→ DeepSeek 吸收 |
| config-backup .gitignore 开锁策略 | 🔐 安全策略 | GLM 评审（锁门后开锁矛盾）→ DeepSeek 吸收 |
| yg-knowledge .gitignore | 🔐 安全增强 | DeepSeek 自检（git add -A 误推） |
| 三步执行验证（Step 8 文件存在+大小检查） | ✅ 质量验证 | DeepSeek 自检 + GLM 评审部分采纳 |
| 变更前快照增加 .env 副本 | 🧱 架构同步 | 与 `.env` 引入配套 |
| 周归档 (Layer 3) 增加 .env.gpg | 🧱 架构同步 | 与 `.env` 引入配套 |
| API Key 加密 → API Key 加密 + `.env` 迁移（Step 2.1 升级） | 🧱 范围扩展 | GLM 评审 → DeepSeek 吸收 |

> *文档版本：v2.2 · 2026-07-29 · 基于 final × GLM-5.1 评审 × DeepSeek 微调 × 先生决策*
> 
> *以"先备份再安全"为第一原则*
